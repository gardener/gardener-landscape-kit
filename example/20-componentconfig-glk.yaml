apiVersion: landscape.config.gardener.cloud/v1alpha1
kind: LandscapeKitConfiguration
# ocm:
#   repositories:
#   - <repo-url>
#   rootComponent:
#     name: <component-name>
#     version: <component-version>
#   originalRefs: true
# git:
#   url: https://github.com/<org>/<repo>
#   ref:
#     branch: <branch-name>
#   paths:
#     base: ./base
#     landscape: ./
# components:
#   exclude:
#   - component-name
# versionConfig:
#   componentsVectorFile: ./versions.yaml
#   # overrideComponentsFile points to a partial components.yaml that overrides specific component
#   # versions on top of the base vector. Only listed components are affected; others keep their
#   # base-vector values. Can be combined with componentsVectorFile (override applies on top).
#   overrideComponentsFile: ./override-versions.yaml
#   defaultVersionsUpdateStrategy: ReleaseBranch

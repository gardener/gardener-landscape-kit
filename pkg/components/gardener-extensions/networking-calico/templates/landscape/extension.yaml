apiVersion: operator.gardener.cloud/v1alpha1
kind: Extension
metadata:
  name: networking-calico
spec:
  deployment:
    admission:
      runtimeCluster:
        helm:
          ociRepository:
            ref: {{ .resources.admissionCalicoRuntime.helmChartRef }}
      {{- if .resources.admissionCalicoRuntime.helmChartImageMap }}
      values:
{{ indent 8 (toIndentYAML 2 .resources.admissionCalicoRuntime.helmChartImageMap.gardenerExtensionAdmissionCalico) }}
      {{- end }}
      virtualCluster:
        helm:
          ociRepository:
            ref: {{ .resources.admissionCalicoApplication.helmChartRef }}
    extension:
      helm:
        ociRepository:
          ref: {{ .resources.networkingCalico.helmChartRef }}
      {{- if (or .resources.networkingCalico.helmChartImageMap .imageVectorOverwrite) }}
      values:
        {{- if .resources.networkingCalico.helmChartImageMap }}
{{ indent 8 (toIndentYAML 2 .resources.networkingCalico.helmChartImageMap.gardenerExtensionNetworkingCalico) }}
        {{- end }}
        {{- if .imageVectorOverwrite }}
        imageVectorOverwrite: |
{{ indent 10 .imageVectorOverwrite }}
        {{- end }}
      {{- end }}

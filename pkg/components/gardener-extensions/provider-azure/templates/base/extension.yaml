apiVersion: operator.gardener.cloud/v1alpha1
kind: Extension
metadata:
  annotations:
    security.gardener.cloud/pod-security-enforce: baseline
  name: provider-azure
spec:
  deployment:
    admission:
      runtimeCluster:
        helm:
          ociRepository:
            ref: europe-docker.pkg.dev/gardener-project/releases/charts/gardener/extensions/admission-azure-runtime:{{ .version }}
      values:
        image:
          repository: europe-docker.pkg.dev/gardener-project/releases/gardener/extensions/admission-azure
          tag: {{ .version }}
#       replicaCount: 1
      virtualCluster:
        helm:
          ociRepository:
            ref: europe-docker.pkg.dev/gardener-project/releases/charts/gardener/extensions/admission-azure-application:{{ .version }}
    extension:
      helm:
        ociRepository:
          ref: europe-docker.pkg.dev/gardener-project/releases/charts/gardener/extensions/provider-azure:{{ .version }}
      injectGardenKubeconfig: true
      runtimeClusterValues:
        image:
          repository: europe-docker.pkg.dev/gardener-project/releases/gardener/extensions/provider-azure
          tag: {{ .version }}
      values:
        controllers:
        image:
          repository: europe-docker.pkg.dev/gardener-project/releases/gardener/extensions/provider-azure
          tag: {{ .version }}
#       replicaCount: 1
  resources:
  - kind: BackupBucket
    type: azure
  - kind: BackupEntry
    type: azure
  - kind: Bastion
    type: azure
  - kind: ControlPlane
    type: azure
  - kind: DNSRecord
    type: azure-dns
  - kind: Infrastructure
    type: azure
  - kind: Worker
    type: azure

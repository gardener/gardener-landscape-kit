apiVersion: operator.gardener.cloud/v1alpha1
kind: Extension
metadata:
  name: networking-cilium
spec:
  deployment:
    admission:
      runtimeCluster:
        helm:
          ociRepository:
            ref: {{ .resources.admissionCiliumRuntime.helmChartRef }}
      {{- if .resources.admissionCiliumRuntime.helmChartImageMap }}
      values:
{{ indent 8 (toIndentYAML 2 .resources.admissionCiliumRuntime.helmChartImageMap.gardenerExtensionAdmissionCilium) }}
      {{- end }}
      virtualCluster:
        helm:
          ociRepository:
            ref: {{ .resources.admissionCiliumApplication.helmChartRef }}
    extension:
      helm:
        ociRepository:
          ref: {{ .resources.networkingCilium.helmChartRef }}
      {{- if (or .resources.networkingCilium.helmChartImageMap .imageVectorOverwrite) }}
      values:
        {{- if .resources.networkingCilium.helmChartImageMap }}
{{ indent 8 (toIndentYAML 2 .resources.networkingCilium.helmChartImageMap.gardenerExtensionNetworkingCilium) }}
        {{- end }}
        {{- if .imageVectorOverwrite }}
        imageVectorOverwrite: |
{{ indent 10 .imageVectorOverwrite }}
        {{- end }}
      {{- end }}

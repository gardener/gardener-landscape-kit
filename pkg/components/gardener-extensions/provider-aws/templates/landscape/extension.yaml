apiVersion: operator.gardener.cloud/v1alpha1
kind: Extension
metadata:
  name: provider-aws
spec:
  deployment:
    admission:
      runtimeCluster:
        helm:
          ociRepository:
            ref: {{ .resources.admissionAwsRuntime.helmChart.ref }}
      {{- if .resources.admissionAwsRuntime.helmChart.imageMap }}
      values:
{{ indent 8 (toIndentYAML 2 .resources.admissionAwsRuntime.helmChart.imageMap.gardenerExtensionAdmissionAws) }}
      {{- end }}
#       resources:
#         requests:
#           cpu: 50m
#           memory: 100Mi
      virtualCluster:
        helm:
          ociRepository:
            ref: {{ .resources.admissionAwsApplication.helmChart.ref }}
    extension:
      helm:
        ociRepository:
          ref: {{ .resources.providerAws.helmChart.ref }}
      {{- if (or .resources.providerAws.helmChart.imageMap .imageVectorOverwrite) }}
      runtimeValues:
        {{- if .resources.providerAws.helmChart.imageMap }}
{{ indent 8 (toIndentYAML 2 .resources.providerAws.helmChart.imageMap.gardenerExtensionProviderAws) }}
        {{- end }}
        {{- if .imageVectorOverwrite }}
        imageVectorOverwrite: |
{{ indent 10 .imageVectorOverwrite }}
        {{- end }}
      values:
        {{- if .resources.providerAws.helmChart.imageMap }}
{{ indent 8 (toIndentYAML 2 .resources.providerAws.helmChart.imageMap.gardenerExtensionProviderAws) }}
        {{- end }}
        {{- if .imageVectorOverwrite }}
        imageVectorOverwrite: |
{{ indent 10 .imageVectorOverwrite }}
        {{- end }}
#       resources:
#         requests:
#           cpu: 100m
#           memory: 512Mi
      {{- end }}
